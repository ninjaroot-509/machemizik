import { popular as popularState } from '../states';

const popular = (state = popularState, { type = null, payload = {} }) => {
	switch (type) {

		default:
			return state;
	}
};

export default popular;
