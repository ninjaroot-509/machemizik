import { recomend as recomendState } from '../states';

const recomend = (state = recomendState, { type = null, payload = {} }) => {
	switch (type) {

		default:
			return state;
	}
};

export default recomend;
